## 1. Login Layout Refinement

- [x] 1.1 Update the login shell styles so the login experience is vertically centered more naturally within the viewport.
- [x] 1.2 Tune login spacing and responsive behavior so the auth form remains balanced on desktop and mobile without drifting toward the top.

## 2. Workspace Shell Refinement

- [x] 2.1 Restyle the primary sidebar into a straighter, full-height workspace navigation panel.
- [x] 2.2 Restyle the settings sidebar into a matching full-height navigation panel for settings pages.
- [x] 2.3 Add a simple shared header region to the authenticated workspace content area in `WorkspaceShell.vue`.

## 3. Content Integration and Verification

- [x] 3.1 Adjust page paddings and content containers so routed agent and settings pages fit cleanly beneath the refined shell header.
- [x] 3.2 Verify the login page, internal agent pages, tenant agent pages, and settings pages all render correctly with the updated shell.
- [x] 3.3 Verify desktop and mobile behavior for full-height sidebars, login centering, and the shared header layout.
